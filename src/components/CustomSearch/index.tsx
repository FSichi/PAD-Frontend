import {
    CustomSearchContainer as CustomSearch,
    MemoizedCustomSearchContainer as CustomSearchMemo,
} from './CustomSearch.container';

export { CustomSearch, CustomSearchMemo };
export default { CustomSearch, CustomSearchMemo };
