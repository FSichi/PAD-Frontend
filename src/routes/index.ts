import { MainRoutes } from './MainRoutes';
import { PrivateRoutes } from './PrivateRoutes';
import { RoutesListPaths, PrivateRoutesList, SidebarRoutes, PublicRoutesList } from './RoutesList';

export {
    MainRoutes,
    PrivateRoutes,
    RoutesListPaths,
    PrivateRoutesList,
    SidebarRoutes,
    PublicRoutesList,
};

export default {
    MainRoutes,
    PrivateRoutes,
    RoutesListPaths,
    PrivateRoutesList,
    SidebarRoutes,
    PublicRoutesList,
};
